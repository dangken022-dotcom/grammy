
export interface RoomInfo {
  id: string;
  name: string;
  type: string;
  description: string;
  price: string;
  features: string[];
  imageUrl: string;
}

export interface Facility {
  title: string;
  description: string;
  icon: string;
}
